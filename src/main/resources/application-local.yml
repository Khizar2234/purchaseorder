server:
  port: 8092
  servlet:
    context-path: /inventory
spring:
  application:
    name: InventoryService
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory?CreateDatabaseIfNotExist=true
    username: postgres
    password: 03022000
    initialization-mode: always
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        namingStrategy: org.hibernate.cfg.PhysicalNamingStrategy
  cloud:
    discovery:
      enabled: false
    compatibility-verifier:
      enabled: false
      
swagger:
  url: http://localhost:8092/inventory
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: https://service-registry.test.restaurantonesolution.com/eureka
  instance:
    hostname: localhost
logging:
  level:
    '[com.ros.inventory]': DEBUG