server:
  port: 8096
  servlet:
    context-path: /purchaseorder
spring:
  application:
    name: InventoryService
  datasource:
    url: jdbc:postgresql://inventory.postgres.database.azure.com:5432/Inventory-Purchaseorder
    username: adminroot
    password: password@123
    initialization-mode: always
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        namingStrategy: org.hibernate.cfg.PhysicalNamingStrategy
  cloud:
    discovery:
      enabled: false
      
swagger:
  url: https://purchaseorder-service.ros.skillassure.com/purchaseorder
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://service-registry.test.restaurantonesolution.com/eureka
  instance:
    hostname: localhost
logging:
  level:
    '[com.ros.inventory]': DEBUG
